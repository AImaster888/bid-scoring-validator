# 評選評分表驗算助手 — 系統設計文件（SDD）

| 項目 | 內容 |
|---|---|
| 專案名稱 | bid-scoring-validator（評選評分表驗算助手） |
| 文件版本 | v1.2（2026-08-21） |
| 對應工具版本 | v1.1.0（2026-08-21） |
| 文件類型 | System Design Document |
| 適用對象 | 政府採購最有利標／公共藝術徵選之評選委員 |

---

## 1. 背景與問題陳述

評選委員在現場評分時，實際面臨的錯誤來源包含：

| 編號 | 問題 | 後果 |
|---|---|---|
| P-01 | 各項分數加總錯誤 | 評分表總分與細項不符，需退回重填 |
| P-02 | 某項給分超過該項配分上限 | 評分表無效 |
| P-03 | 漏填某一項 | 評分表不完整 |
| P-04 | 未察覺總分落在及格門檻邊緣 | 入選與否判斷失誤 |
| P-05 | 同一委員對不同廠商的落點與自身評語描述不一致 | 申訴時難以說明 |
| P-06 | 心中的整體印象分與逐項填出的加總有落差，未被察覺 | 評分表無法反映真實判斷 |

本系統針對 P-01 至 P-06 提供輔助，**不介入實質評分判斷**。

---

## 2. 設計原則

### 2.1 核心原則：分數必須來自委員

系統的所有輸出皆由委員輸入的細項分數推導而得。系統在任何情況下都不產生評分。

### 2.2 法規對齊

| 依據 | 要求 | 本系統的對應 |
|---|---|---|
| 政府採購法 | 評選程序應公平、公正 | 系統不介入判斷，僅驗算 |
| 最有利標評選辦法 | 委員應就各評選項目評分 | 採逐項輸入，總分為加總結果 |
| 採購評選委員會審議規則 | 評分表為正式評選紀錄 | 產出可直接對應制式表格 |
| 公共藝術設置辦法 第31條 | 執行小組委員之利益迴避 | 系統不儲存、不傳輸任何資料 |

### 2.3 承認「整體印象先行」的現實

評分實務上，委員常在簡報結束時即形成整體印象，再回頭分配細項。本系統不否定此流程，而以「錨定對帳模式」（模式 B）支援之：委員可先記錄整體印象分作為錨點，逐項填寫後由系統顯示差距，**由委員自行決定調整哪一項**。

---

## 3. 範圍

### 3.1 實作範圍

- 評分項目、配分與配分合計之設定（合計預設 100，可覆寫）
- 逐項分數輸入與自動加總
- 輸入驗算與警示
- 及格門檻與提醒區間標示
- 錨定對帳（整體印象分 vs 細項加總之差距提示）
- 跨廠商落點一致性提示（依委員自填之強／中／弱標記比對序位）
- 評分表匯出（Markdown / CSV）
- 範例模式標記

### 3.2 明確不實作之功能

| 功能 | 不實作之理由 |
|---|---|
| 由總分自動產生各項分數 | 產生的數字不來自委員對提案的判斷，該評分表無法反映實際評選過程；若廠商提出異議或申訴，此類紀錄不利於委員與機關 |
| 「各家不得等比例拆分」之落點擾動 | 此規則的功能為使自動產生的分數在外觀上近似逐項評分結果，屬外觀修飾而非評分輔助 |
| 依廠商名稱推薦分數 | 逾越工具定位，且有偏頗之虞 |
| 任何形式的雲端儲存或上傳 | 評選資料具保密性 |

上述限制為設計決策，非技術限制。提出 PR 加入此類功能者將不予合併，理由同上。

---

## 4. 系統架構

### 4.1 部署形式

```
bid-scoring-validator/
├── index.html          # 單一檔案版本（HTML + CSS + JS 內嵌）
├── prompt/
│   └── zh-TW.md        # LLM 版本提示詞（供不便使用瀏覽器者）— 規劃中，尚未實作
├── examples/
│   └── sample.md       # 範例評分表（標記為模擬資料）— 規劃中，尚未實作
├── README.md
├── DESIGN.md           # 本文件
└── LICENSE
```

### 4.2 技術決策

| 決策 | 選擇 | 理由 |
|---|---|---|
| 執行環境 | 純前端，單一 HTML 檔 | 委員可下載後離線使用，無需安裝 |
| 網路請求 | 無 | 評選資料不得外流；離線亦可運作 |
| 資料儲存 | 僅記憶體（不使用 localStorage） | 關閉分頁即清除，避免資料殘留於公用電腦 |
| 相依套件 | 無 | 降低供應鏈風險，便於機關資安審查 |

### 4.3 資料流

```
[委員輸入設定] → 設定驗算 → [設定確認表]
                                  ↓
[委員逐項輸入分數] → 即時驗算 → [警示訊息]
                                  ↓
                            自動加總 → [總分]
                                  ↓
                    門檻比對 / 錨點對帳 / 一致性檢查
                                  ↓
                            [評分表輸出]
```

---

## 5. 資料模型

```javascript
Config = {
  caseName: String,              // 案名，可留空
  criteria: [                    // 評分項目
    { id: 1, name: String, maxScore: Number }
  ],
  vendors: [                     // 廠商
    { id: 1, label: String }
  ],
  totalPoints: Number,           // 配分合計（預設 100，可覆寫）
  passThreshold: Number,         // 及格門檻
  alertMargin: Number,           // 提醒區間寬度（預設 2）
  isSampleMode: Boolean          // 範例模式
}

Score = {
  vendorId: Number,
  criterionId: Number,
  value: Number | null           // null 表示未填
}

Anchor = {                       // 模式 B 專用，選填
  vendorId: Number,
  impressionScore: Number,       // 委員的整體印象分（目標分）
  note: String                   // 委員自述之強弱項，自由文字，不參與 F-06 檢查
}

Impression = {                   // F-06 專用，選填
  vendorId: Number,
  criterionId: Number,
  level: 'strong' | 'mid' | 'weak' | null   // 強／中／弱／未填
}
```

**關於 `totalPoints`**：多數機關評分表以 100 分為滿分，故預設 100；但部分評分表另有加分項或採其他分制，故開放於設定階段覆寫。覆寫後 V-05、V-06 及目標分上限一律改以 `totalPoints` 為準。

---

## 6. 功能規格

### F-01 設定精靈

依序提問，一次一題，未完成不得進入評分階段。

| 序 | 提問 | 驗算 |
|---|---|---|
| Q0 | 案名？（可留空） | 不驗算；僅用於匯出標題與檔名 |
| Q1 | 共有幾個評分項目？ | 需為 1–10 之整數 |
| Q2 | 依序輸入各項名稱 | 可留空；留空者自動命名為「項目 N」。自行填寫時不得重複 |
| Q3 | 本表配分合計為幾分？（預設 100） | 需為 1–1000 之正數 |
| Q4 | 依序輸入各項配分 | 見 V-05 |
| Q5 | 共有幾家廠商？名稱或代號為何？ | 需為 1–20 家 |
| Q6 | 及格門檻幾分？ | 見 V-06 |
| Q7 | 提醒區間寬度？（預設門檻 +2 分） | 0–10 |
| Q8 | 是否為範例／教學用途？ | 是則啟用範例模式 |

Q3 直接確認即採用預設值 100。Q0 留空時，匯出之 Markdown 標題為「評分表」，CSV 檔名不含案名段。

**關於 Q2 可留空**：本系統為算分與驗算工具，評分項目名稱僅影響畫面標示與匯出表格的可讀性，不參與任何計算。強制輸入名稱會在設定階段拖慢流程，故改為可留空並自動命名。委員若需與機關制式表格對應，仍可自行填寫實際項目名稱。

完成後輸出「設定確認表」供核對，確認後方進入評分階段。

### F-02 逐項輸入與自動加總

委員逐項填入分數，系統即時加總。總分為唯讀欄位，不可直接編輯。

**修改行為**：任一項分數變更時，該廠商總分立即重算，門檻標示同步更新。

**實例**

| 項目 | 配分 | 修改前 | 修改後 |
|---|---|---|---|
| 提案之可行性及專業度 | 50 | 41 | 41 |
| 民眾參與及教育推廣方案 | 25 | 19 | 19 |
| 經費與進度合理性 | 15 | 13 | 13 |
| 現場簡報答詢 | 10 | **8** | **9** |
| 合計 | 100 | 81 ⚠ | 82 ⚠ |

門檻 80、提醒區間 80–82：修改後總分 82 仍落在區間內，⚠ 標示保留。若再加 1 分至 83，標示解除。

此即問題 P-01 之典型情境——四項心算差一分不易察覺，故總分必須由系統計算，不接受人工輸入（見 V-07）。

### F-03 即時驗算

見第 7 節驗算規則。任一驗算未通過時，於該欄位旁顯示警示，並禁止匯出。

### F-04 門檻標示

| 條件 | 標示 |
|---|---|
| 總分 < 門檻 | ✕ 未達門檻 |
| 門檻 ≤ 總分 ≤ 門檻 + alertMargin | ⚠ 落在提醒區間，請確認 |
| 總分 > 門檻 + alertMargin | （無標示） |

### F-05 錨定對帳（選用）

用於支援「整體印象先行」的實際評分流程。

**版面**

每家廠商的欄位底部並列兩個數值，視覺上明確區分：

```
合計（自動計算）   81      ← 唯讀，灰底
目標分（可編輯）  [ 84 ]   ← 可輸入，白底
差距              +3       ← 自動顯示
```

**行為**

1. 目標分可於任何時候輸入或修改，不限評分前後；上限為 `totalPoints`
2. 目標分變動時，差距即時更新；**細項分數不受影響**
3. **差距 = 目標分 − 合計**。差距為正表示目標分高於細項加總，為負則反之。畫面與 Markdown 顯示正負號（`+3` / `-3`）；CSV 為便於後續運算，正數不加正號（輸出 `3` / `-3`）
4. 差距 ≠ 0 時，展開「可調整空間」參考表：

   | 項目 | 目前 | 上限 | 尚可加 |
   |---|---|---|---|
   | 提案之可行性及專業度 | 41 | 50 | +9 |
   | 民眾參與及教育推廣方案 | 19 | 25 | +6 |
   | 經費與進度合理性 | 13 | 15 | +2 |
   | 現場簡報答詢 | 8 | 10 | +2 |

5. **系統僅顯示空間，不建議調整哪一項，亦不代為填入**
6. 委員自行修改細項後，合計與差距即時重算
7. 差距歸零時顯示 `✓ 已對齊`

**若委員選擇不對齊**

允許保留差距。匯出時以合計（細項加總）為準，目標分另欄記載，並註明「目標分僅供對帳參考」。

**設計說明**

本功能承認委員常於簡報結束時即形成整體判斷，再回頭分配細項。差別在於細項數字由委員決定，而非由系統生成——前者是判斷的拆解，後者是紀錄的製造。故目標分欄位不觸發任何自動填值。

### F-06 跨廠商一致性提示

當同一委員對不同廠商的同一項目給分順序，與其自行標記的強弱等級不一致時，顯示提示。

**強弱標記**

每個分數格旁附一個下拉選單，值域 `強／中／弱／—`（`—` 為未填）。標記全部為選填，不影響任何計算與匯出分數。

為避免表格過擠，標記欄**預設隱藏**，由表頭「顯示強弱標記」開關切換。

**檢查規則**

逐項目比對該項目下所有已填標記的廠商配對。若甲的等級強於乙，但甲的分數 ≤ 乙的分數（含同分），則提示：

> 「現場簡報答詢」：甲（中）8 分、丙（弱）8 分，等級不同但分數相同，請確認。

**設計說明**

原規格構想為判讀委員文字附註的語意強弱，惟本系統採零相依、零網路、純前端（見 4.2），不具語意分析能力。改以委員自行選擇的結構化等級進行純序位比對：判斷仍完全來自委員，系統只負責指出序位矛盾。

`Anchor.note`（模式 B 之自述強弱項）維持自由文字，不參與此檢查。

此為提示，不阻擋作業，亦不代為調整任何分數。

### F-07 匯出

提供三種輸出，皆為前端產生，不經任何伺服器。

| 方式 | 用途 |
|---|---|
| 下載 CSV | 匯入 Excel／機關系統 |
| 下載 Markdown | 貼入文件、會議紀錄 |
| 複製到剪貼簿 | 直接貼入表單欄位 |

#### CSV 規格

- 編碼：**UTF-8 with BOM**（`\uFEFF` 開頭）
  — 不加 BOM 時，Windows 版 Excel 開啟中文 CSV 會出現亂碼
- 換行：CRLF
- 分隔：半形逗號；欄位值含逗號或引號時以雙引號包覆並跳脫
- 檔名：有案名時為 `評分表_{案名}_{YYYYMMDD_HHmm}.csv`，未填案名時為 `評分表_{YYYYMMDD_HHmm}.csv`（案名與時間資訊放在檔名，不佔用內容列）
  — 案名中的 `\ / : * ? " < > |` 及控制字元一律以底線取代，避免產生無效檔名

**版面：直欄為廠商，橫列為項目**（與畫面一致，便於貼入制式表格）

```csv
項目,配分,甲,乙,丙
提案之可行性及專業度,50,41,37,44
民眾參與及教育推廣方案,25,19,18,21
經費與進度合理性,15,13,14,13
現場簡報答詢,10,8,9,6
合計,100,81,78,84
目標分,,84,,
差距,,3,,
狀態,,提醒區間,未達門檻,通過
```

**欄位規則**

- 不輸出任何中繼資料列，第一列即為表頭，可直接貼入試算表或制式表格
- 「合計」列的配分欄輸出 `totalPoints`（預設 100，經覆寫時為覆寫值）
- 「目標分」「差距」兩列僅在該廠商有輸入目標分時填值，否則留空；三家皆未輸入時，此二列整列省略
- 「差距」為 目標分 − 合計；正數不加正號，負數保留負號（例：`3`、`-3`）
- 「狀態」值域：`通過` / `提醒區間` / `未達門檻`
- 分數欄位為純數字，不含 ⚠ ✕ 等符號，以利後續運算
- 強弱標記（F-06）不輸出至 CSV，僅供畫面提示使用
- 範例模式為唯一例外：第一列強制輸出 `※ 本表為範例／模擬資料，非實際評選結果`

#### Markdown 規格

```markdown
## 評分表：潮州二期公共藝術 A-2

匯出時間：2026-08-21 14:30
及格門檻：80 分（提醒區間 80–82）

| 項目 | 配分 | 甲 | 乙 | 丙 |
|---|---|---|---|---|
| 提案之可行性及專業度 | 50 | 41 | 37 | 44 |
| 民眾參與及教育推廣方案 | 25 | 19 | 18 | 21 |
| 經費與進度合理性 | 15 | 13 | 14 | 13 |
| 現場簡報答詢 | 10 | 8 | 9 | 6 |
| **合計** | **100** | **81** ⚠ | **78** ✕ | **84** |

⚠ 甲 81 分，落在提醒區間（80–82）
✕ 乙 78 分，未達門檻 80 分

名次：丙（84）→ 甲（81）→ 乙（78，不入選）
```

#### 共通規則

- Markdown 標題為 `## 評分表：{案名}`；案名留空時為 `## 評分表`
- 匯出前執行 V-01～V-04，任一未通過即阻擋並列出缺漏欄位
- 範例模式下，兩種格式之開頭皆強制加註：

```
※ 本表為範例／模擬資料，非實際評選結果
```

  此為唯一不可關閉的附加內容，用於避免測試或教學用表格日後被誤認為正式評選紀錄。

### F-08 重置

單一「全部重設」按鈕，清除所有分數與設定，回到設定精靈 Q0。

**規則**

- 清除範圍：所有廠商之各項給分、錨點、附註、強弱標記，以及案名、評分項目、配分、配分合計、廠商名單、門檻設定
- 不可復原，不提供 undo
- 執行前顯示確認對話框：`將清除所有分數與設定，回到最初設定。此動作無法復原，確定嗎？`
- 重置後不保留任何殘留資料（本系統本即不使用 localStorage，關閉分頁亦自動清除）

---

## 7. 驗算規則

| 編號 | 規則 | 觸發時機 | 處理 |
|---|---|---|---|
| V-01 | 各項給分不得超過該項配分 | 輸入時 | 阻擋並警示 |
| V-02 | 各項給分不得為負 | 輸入時 | 阻擋並警示 |
| V-03 | 各項給分需為數字 | 輸入時 | 阻擋並警示 |
| V-04 | 所有項目皆須填寫 | 匯出前 | 阻擋匯出，標示缺漏欄位 |
| V-05 | 各項配分合計須等於 `totalPoints`（預設 100，可於設定時覆寫） | 設定時 | 阻擋並回報差額 |
| V-06 | 及格門檻須 ≥ 0 且 ≤ `totalPoints` | 設定時 | 阻擋並警示 |
| V-07 | 總分 = 各項加總（系統計算，不接受外部輸入） | 恆時 | 唯讀欄位 |

---

## 8. 使用流程

```
1. 下載 index.html，以瀏覽器開啟（可離線）
2. 依設定精靈完成 Q0–Q8
3. 核對設定確認表
4. （選用）輸入各家整體印象分作為錨點
5. 逐項填寫各家分數
6. （選用）開啟強弱標記，標註各項強／中／弱
7. 檢視警示訊息，修正至無警示
8. （選用）檢視錨點差距，自行決定是否調整
9. 匯出評分表
10. 謄寫或列印至機關制式評分表，簽名
11. 關閉分頁（資料自動清除）
```

**注意**：本系統之輸出為輔助用途。正式評選紀錄仍以機關提供之制式評分表為準，並由委員親自簽名。

---

## 9. 錯誤處理

| 情境 | 處理方式 |
|---|---|
| 設定階段配分合計 ≠ `totalPoints` | 顯示 `目前合計 ○ 分，與 ○ 分相差 ○ 分`，不進入下一步 |
| 及格門檻超出 0–`totalPoints` | 欄位標紅，顯示 `門檻須介於 0 至 ○ 分` |
| 分數超過配分上限 | 欄位標紅，顯示 `本項上限 ○ 分` |
| 匯出時有缺漏 | 列出所有未填欄位，不執行匯出 |
| 瀏覽器不支援 | 顯示提示，建議改用 prompt 版 |

---

## 10. 安全與保密

- 無任何網路請求；可於斷網環境使用
- 不使用 localStorage / sessionStorage / cookie
- 不記錄、不傳輸、不分析任何輸入內容
- 建議委員使用完畢後關閉分頁；若使用公用電腦，另建議清除瀏覽紀錄
- 原始碼公開，機關資安單位可自行審查

---

## 11. 測試案例

| 編號 | 情境 | 預期結果 |
|---|---|---|
| T-01 | 配分輸入 50/25/15/10 | 通過，合計 100 |
| T-02 | 配分輸入 50/25/15/15 | 阻擋，提示相差 5 分 |
| T-03 | 「簡報答詢」（上限 10）輸入 12 | 阻擋，提示上限 10 分 |
| T-04 | 輸入 41/19/13/8 | 總分顯示 81 |
| T-05 | 門檻 80、區間 2，總分 81 | 顯示 ⚠ 提醒 |
| T-06 | 門檻 80，總分 78 | 顯示 ✕ 未達門檻 |
| T-07 | 錨點（目標分）82、細項加總 79 | 顯示差距 +3，不自動調整 |
| T-08 | 某項留空後匯出 | 阻擋，標示該欄位 |
| T-09 | 啟用範例模式後匯出 | 輸出含模擬資料標記 |
| T-10 | 嘗試直接編輯總分欄位 | 欄位唯讀，無法編輯 |
| T-11 | 配分合計覆寫為 110，各項輸入 50/30/20/10 | 通過，合計 110 |
| T-12 | 同一項目標記甲「中」8 分、丙「弱」8 分 | 顯示 F-06 一致性提示，不阻擋 |
| T-13 | 匯出 CSV | 首字元為 `﻿`，換行為 CRLF |

T-01～T-13 之純函式層（驗算、加總、匯出字串產生）內建於 `index.html`，以 `index.html?selftest=1` 開啟即執行並於頁面列出結果。不引入任何測試框架，維持零相依。

---

## 12. 未來擴充（暫不排程）

- 多委員彙整與平均分計算（須留意採購法對委員個別評分保密之要求）
- 評分表 PDF 直出
- 英文介面

---

## 13. 版本歷程

| 文件版本 | 日期 | 對應工具版本 | 變更摘要 |
|---|---|---|---|
| v1.2 | 2026-08-21 | v1.1.0 | F-01 Q2 評分項目名稱改為可留空，留空者自動命名為「項目 N」；名稱不參與計算，不應阻擋設定流程 |
| v1.1 | 2026-08-21 | v1.0.0 | 配分合計改為可覆寫（新增 `Config.totalPoints`，預設 100）；F-06 改以委員自填之強／中／弱標記做序位比對，移除語意判讀要求；新增案名欄位（Q0）；定義差距 = 目標分 − 合計並統一正負號；修正 F-07 子節與第 7 節之編號衝突；新增測試案例 T-11～T-13 |
| v1.0 | — | — | 初版設計文件 |

---

## 14. 授權與免責

- 授權：MIT License
- 本工具為評分輔助與驗算用途，不構成法律意見
- 正式評選紀錄之效力，以機關制式表格與委員簽名為準
- 使用者應自行確認符合所屬機關之評選規定
